# Copy to .env — SQLite works locally. For production (Vercel, etc.), use Postgres (Neon, Supabase) and update prisma/schema.prisma provider.
DATABASE_URL="file:./dev.db"

# Optional: comma-separated origins for CORS in API (defaults to same-origin in browser)
# ALLOWED_ORIGINS="https://yourdomain.com"
